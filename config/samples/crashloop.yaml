apiVersion: v1
kind: Pod
metadata:
  name: sample-crashloop
  namespace: default
  labels:
    app: sample-crashloop
spec:
  restartPolicy: Always
  containers:
    - name: app
      image: busybox:1.36
      command: ["/bin/sh", "-c", "exit 1"]
